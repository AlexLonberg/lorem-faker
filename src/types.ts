/**
 * Диапазон в виде кортежа двух числовых значений.
 */
type TRange = [number, number]

/**
 * Структура с минимальным и максимальным числовым полем.
 */
type TMinMax = { min: number, max: number }

export {
  type TRange,
  type TMinMax
}
